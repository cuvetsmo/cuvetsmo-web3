import { Placeholder } from "@/components/ui/placeholder";

export const metadata = {
  title: "Templates",
  description: "Gallery of Lab templates — fork, customize, deploy.",
};

export default function TemplatesPage() {
  return (
    <Placeholder
      agent="Agent D (The Lab factories)"
      pillar="The Lab"
      title="Templates"
      description="Gallery of pre-built DApp templates: club voting, event ticketing, badge collection, fundraiser. Fork → customize → deploy."
      spec="master plan §7.6 Templates"
    />
  );
}
